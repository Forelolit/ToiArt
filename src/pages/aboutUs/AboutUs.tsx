import { Container } from '@/components';
import type { FC } from 'react';

export const AboutUs: FC = () => {
    return (
        <section>
            <Container>О нас</Container>
        </section>
    );
};
